ToDO

Critical
	+ Port shadertoy fully
	+ Split diffuse and lighting into separate EXR channels

* Mplay
	- Export to mp4 using ffmpeg?

* Camera
	- Basic keyframing, list of keyframes at certain frames? Store xform

* Lights
	+ Add lights to list
	+ Control colour and intensity
	+ Visibility?
	+ Look through light to position it
* Port over shadertoy test
	+ Distance Field Header

* Toggle between basic phong shading model and path tracing
* Render
	+ Shots are saved in separate callable cuda files
	+ Command line options for batch rendering
		- Which scene to render
		- Start/End Frame
		- Option to auto-quit when complete (render sequence of shots)
	+ Add option to change render path in gui or via command line